[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "kernel"
path = "src/main.rs"

[dependencies]
bitflags = { version = "2", default-features = false }
limine = "0.6"

[profile.dev]
panic = "abort"

[features]
# Build the kernel with a deliberately invalid registry ELF to trigger the TCB
# failure path in init (§6.2, §22 Test 1B).  Only used by `osdev test identity`.
test-bad-registry = []

[profile.release]
panic = "abort"
