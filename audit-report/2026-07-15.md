<!-- SPDX-License-Identifier: GPL-2.0-only -->
# Audit Report - 2026-07-15

Branch `feat/aarch64-prep` @ `a9c8566` &middot; Scope: **full trilogy** (kernel + userspace + documentation)
&middot; 13 auditors (10 code on a capable model, 3 documentation probes on the least-capable model).

This is a dashboard. The system of record is the three living audit docs; this report reconciles from them.

## Summary

| Audit | Scope | HIGH | MED | LOW | Status |
|-------|-------|:----:|:---:|:---:|:------:|
| **Kernel** (`kernel-audit.md` Audit 4) | 5 subsystems | 0 | 1 | 4 | ⚠️ 1 MED-HIGH staged |
| **Userspace** (`userspace-audit.md` Audit 3) | 5 groups | 0 | 5 (+1 carried) | 3 | ⚠️ staged |
| **Documentation** (`documentation-audit.md` Audit 2) | 3 probes | 0 | 0 | 1 | ✅ fixed |

**Overall: no HIGH, no panic/wedge/corruption/authority-escape on any path.** The findings are
resource-lifecycle gaps (frames/caps/slots not reclaimed on some failure paths) and one recovery
backstop gap - all concrete and ordinary-use-reachable, none a hang or corruption. The arch demarcation
that dominates this branch is verified **sound** (zero boundary leaks; a mechanical seam extraction with
no logic change). The documentation passed: the prior audit's fixes are confirmed legible to a cold
weak model, and the field guide works as a reviewer checklist.

## Kernel - `docs/kernel-audit.md`, Audit 4

| syscall+ipc+interrupt | capability | task+scheduler | memory+smp | arch+boundary-seam |
|:-:|:-:|:-:|:-:|:-:|
| ✅ | ✅ (2 LOW) | ⚠️ **T1** | ✅ (2 LOW) | ✅ 0 leaks |

Re-verified present-and-correct: C1/C2 (ring-3 fault kill vs halt), K1/K3, V3 (scheduler UAF handshake),
C3 (non-panic respawn). Boundary integrity checked four ways (CI guard + independent greps).

## Userspace - `docs/userspace-audit.md`, Audit 3

| block-driver+fs | nic-driver+net-stack | supervisor+logger | xhci+ehci | shell + chaos/observe/probe |
|:-:|:-:|:-:|:-:|:-:|
| ⚠️ **F1** | ⚠️ **N1** (+M4 carried) | ⚠️ **S1** | ⚠️ **XH-1 XH-2** (+3 LOW) | ✅ |

All prior Audit 1/2 fixes re-verified present in source. No `unsafe` in any service; no shared mutable
statics. The two USB MEDs live in the newer, not-yet-hardware-tested hub-walk code; the root-port path
is clean.

## Documentation - `docs/documentation-audit.md`, Audit 2

| grokability (median) | Audit-1 fixes legible? | seeded review (field guide) |
|:-:|:-:|:-:|
| **7/10** ✅ | ✅ all confirmed | ✅ caught cluster + cited `anti-patterns.md` |

Both cold reads independently answered the fix-area questions correctly (log_fmt, GRANT three-checks,
the §14.3 stale-handle rule, the anti-patterns location, the `#[no_mangle]` gotcha). Comprehension maxed,
coherence 9/10. One new LOW (amendment-shorthand glossary) - fixed this run.

## Findings ledger (MED and above; full detail + LOWs in the living docs)

| ID | Sev | Where | Finding | Status |
|----|-----|-------|---------|--------|
| **T1** | MED-HIGH | kernel `task/mod.rs` | Page-table + ELF + stack frames leak on a spawn failure after `loader::load()`; ratchets under `poll_supervisor_respawn` retries. §26.6/IX (not a wedge). | open, staged (fix specified) |
| **F1** | MED | `fs` | `OP_RESET`/`OP_FLASH` drop the volume without revoking open file caps -> delegated-slot leak. | open, staged |
| **N1** | MED | `net-stack` | Sockets never closed/revoked -> 8-slot table leaks; 9th open fails with a misleading "no NIC" error. | open, staged |
| **S1** | MED | `supervisor` | Logger's fresh-boot spawn bypasses the name-map, silently disabling its dropped-notification recovery backstop. | open, staged |
| **XH-1** | MED | `xhci` | HC-wedge detection not ported to the hub/downstream path; a downstream wedge can re-freeze all cores. | open, staged |
| **XH-2** | MED | `xhci` | Hub-walk Address-Device failure leaks a controller slot (no `disable_slot`). | open, staged |
| **M4** | MED | `net-stack` | (carried) `!have_mac` gate never re-reconciles a cached IP on a different-subnet re-DHCP. | deferred |

## Fixed this run (safe, doc-only)

- **DA1** - amendment-shorthand glossary added to `GLOSSARY.md`.
- **M1/M2 doc-drift** - `memory/CLAUDE.md` + `task/CLAUDE.md` repointed at the live mechanisms
  (`scheduler.rs` alloc arrays, `page_tables::reclaim_user_frames`, `resolve_spawn_core`); the dead-code
  deletion itself is staged.

## Next actions (remediation, staged)

Fix HIGH/MED-first, on branch, each verified before landing: **T1** (reclaim frames in
`cleanup_partial_spawn`), then **F1/N1/S1** (revoke-on-teardown + logger name-map), then **XH-1/XH-2**
(port wedge-detection + slot-disable to the hub-walk), then the LOW dead-code deletions (M1/M2/K-a) and
USB observability nits (XH-3/4/5). M4 stays deferred (needs a multi-subnet network).
