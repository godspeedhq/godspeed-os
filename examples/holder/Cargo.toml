[package]
name = "holder"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "holder"
path = "src/main.rs"

[dependencies]
godspeed = { path = "../../stdlib/rust" }
# The hand-rolled `invoke` still names the SDK - deliberately, see its doc comment.
godspeed-sdk = { path = "../../sdk/rust" }
