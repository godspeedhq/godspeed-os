# target: examples/00-hello/CLAUDE.md
# mode: append
# checker: scripts/dash_check.py
# why: The DECIDABLE class. One right answer, no reader needed - so `conform` fixes it and reports one
# why: line rather than a full frame. A fixable violation rendered as a full diagnostic whose help says
# why: "conform fixes this" is the least interesting problem taking the most space, which trains a
# why: reader to skim past the ones that matter.
--- plant ---

A trailing note \u2014 with an em-dash in it.
--- expect ---
error[GS0101]: an em-dash or en-dash appears in a tracked text file
    = note: 1 file, listed above; `py scripts/conform.py` fixes it
