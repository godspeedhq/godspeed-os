name    = "logger"
version = "0.1.0"

[resources.memory]
request = "4MiB"
limit   = "8MiB"

[capabilities]
ipc_receive = ["logger"]
# logger needs hw access to write to the serial port.
hw_mmio     = ["0x3F8+0x8"]

[placement]
core = 0
